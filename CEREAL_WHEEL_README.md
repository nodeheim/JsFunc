# 🥣 3D 시리얼 원판 인터랙티브

Three.js를 사용한 인터랙티브 3D 시리얼 원판 애니메이션입니다.

## 주요 기능

### 🎯 핵심 기능
- **3D 원판**: 회전하는 원형 디스크
- **시리얼 파티클**: 다양한 색상과 모양의 시리얼이 떨어짐
- **와이퍼 암**: 회전하며 시리얼을 밀어냄
- **물리 시뮬레이션**: 중력, 충돌, 마찰 효과
- **인터랙티브 컨트롤**: 마우스/터치로 시점 조작

### 🎨 시리얼 종류 (8가지)
1. 딸기볼 (빨강)
2. 콘플레이크 (노랑)
3. 초코볼 (초록)
4. 블루베리 (파랑)
5. 오렌지 (주황)
6. 그레이프 (보라)
7. 마시멜로 (핑크)
8. 허니넛 (금색)

## 아임웹에서 사용하기

### 방법 1: HTML 위젯으로 삽입

1. 아임웹 편집기에서 **HTML 위젯** 추가
2. `cereal-wheel.html` 파일의 전체 코드를 복사
3. HTML 위젯에 붙여넣기
4. 저장 및 미리보기

### 방법 2: 팝업으로 사용

1. 아임웹에서 **팝업 만들기**
2. 팝업 내용에 **HTML 위젯** 추가
3. `cereal-wheel.html` 코드 붙여넣기
4. 버튼 클릭 시 팝업 열리도록 설정

### 방법 3: 전체 페이지로 사용

1. 새 페이지 생성
2. **코드 직접 입력** 모드로 전환
3. `cereal-wheel.html` 전체 코드 붙여넣기
4. 페이지 저장

## 조작 방법

### 🖱️ 마우스 컨트롤
- **드래그**: 시점 회전
- **스크롤**: 줌 인/아웃

### 📱 터치 컨트롤 (모바일)
- **스와이프**: 시점 회전
- **핀치**: 줌 인/아웃

### 🎮 제어판 버튼
- **시리얼 추가**: 20개의 새 시리얼 추가
- **모두 제거**: 모든 시리얼 제거
- **와이퍼 시작/정지**: 와이퍼 회전 토글
- **원판 회전**: 원판 회전 토글
- **와이퍼 속도**: 슬라이더로 속도 조절
- **원판 속도**: 슬라이더로 속도 조절

## 커스터마이징 가이드

### 색상 변경

```javascript
// 배경 그라데이션 (CSS 부분)
background: linear-gradient(135deg, #1e3c72 0%, #2a5298 50%, #7e22ce 100%);

// 원판 색상 (JavaScript 부분)
const discMaterial = new THREE.MeshPhongMaterial({
    color: 0x2d3561,  // 원하는 색상 코드
});
```

### 시리얼 개수 조절

```javascript
// 초기 시리얼 개수 (init 함수 내)
addCereals(50);  // 숫자 변경

// 버튼 클릭 시 추가되는 개수
addCereals(20);  // 숫자 변경
```

### 원판 크기 변경

```javascript
// createDisc 함수에서
const discGeometry = new THREE.CylinderGeometry(
    5,    // 상단 반지름
    5,    // 하단 반지름
    0.3,  // 높이
    64    // 세그먼트
);
```

### 와이퍼 속도 기본값

```javascript
let wiperSpeed = 2;  // 원하는 속도 (0.5 ~ 5)
let discSpeed = 0.5;  // 원하는 속도 (0 ~ 3)
```

## 성능 최적화

### 권장 설정
- **시리얼 개수**: 50~100개 (기본 50개)
- **브라우저**: Chrome, Edge, Safari 최신 버전
- **해상도**: 1920x1080 이상에서 최적

### 모바일 최적화
- 자동으로 반응형 디자인 적용
- 터치 제스처 지원
- 성능에 따라 시리얼 개수 조절 권장

## 기술 스택

- **Three.js r128**: 3D 렌더링
- **Vanilla JavaScript**: 물리 시뮬레이션
- **CSS3**: 스타일링 및 UI

## 브라우저 호환성

- ✅ Chrome 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ✅ Edge 90+
- ✅ 모바일 브라우저 (iOS Safari, Chrome Mobile)

## 라이선스

이 프로젝트는 자유롭게 사용 및 수정 가능합니다.

## 문의

문제가 있거나 개선 사항이 있다면 이슈를 등록해주세요!
